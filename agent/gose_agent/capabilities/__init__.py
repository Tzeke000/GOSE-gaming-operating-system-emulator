"""Agent capabilities: input, system, games, screen.

Each module exposes a capability class plus a factory that auto-selects a real or
mock backend based on the environment (and config.force_mock).
"""
